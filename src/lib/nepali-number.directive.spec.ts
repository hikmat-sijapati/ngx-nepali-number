import { NepaliNumberDirective } from './nepali-number.directive';

describe('NepaliNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new NepaliNumberDirective();
    expect(directive).toBeTruthy();
  });
});
